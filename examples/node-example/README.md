# zkap-circuit Node.js Example

Demonstrates ZK proof generation using the NAPI binding.

## Prerequisites
- Node.js >= 18
- Built NAPI binding (run `./build.sh --napi-only` from project root)
- Generated proving keys (run `./build.sh --keys-only` from project root)

## Setup
npm install

## Run
node index.js

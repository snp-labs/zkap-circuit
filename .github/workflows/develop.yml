name: Deploy zkup to proof worker server (Develop)

permissions:
  contents: read
  id-token: write

on:
  workflow_dispatch: # 수동 실행만 가능
    inputs:
      version:
        description: '배포할 버전 (예: 0.0.1)'
        required: true
        type: string

jobs:
  deploy-to-vm:
    name: Deploy to VM
    runs-on: self-hosted
    environment: development
    steps:
      - name: Authenticate to Google Cloud
        id: auth
        uses: google-github-actions/auth@v2  # TODO: pin to SHA — check https://github.com/google-github-actions/auth/releases/tag/v2
        with:
          workload_identity_provider: ${{ vars.GCP_WIF_PROVIDER }}
          service_account: ${{ vars.GCP_WIF_SERVICE_ACCOUNT }}
          audience: ${{ vars.GCP_WIF_AUDIENCE }}
          token_format: "access_token"
          create_credentials_file: true

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2  # TODO: pin to SHA — check https://github.com/google-github-actions/setup-gcloud/releases/tag/v2

      - name: Deploy from bucket to Worker VM
        run: |
          BUCKET_NAME=$(echo "${{ vars.GCP_BUCKET_NAME }}" | tr -d ' \n\r')
          VM_INSTANCE=$(echo "${{ vars.GCP_VM_WORKER_INSTANCE_NAME }}" | tr -d ' \n\r')
          VM_ZONE=$(echo "${{ vars.GCP_VM_ZONE }}" | tr -d ' \n\r')
          ZKUP_DIR=$(echo "${{ vars.GCP_VM_ZKUP_DIR_NAME }}" | tr -d ' \n\r')
          VERSION="${{ inputs.version }}"
          FOLDER_NAME="zkup_${VERSION}"
          
          echo "버킷에서 파일을 Worker VM으로 배포합니다..."
          echo "버킷: $BUCKET_NAME"
          echo "VM 인스턴스: $VM_INSTANCE"
          echo "VM Zone: $VM_ZONE"
          echo "버전: $VERSION"
          echo "폴더명: $FOLDER_NAME"
          
          # 임시 디렉토리에 버킷에서 파일 다운로드
          TEMP_DIR=$(mktemp -d)
          echo "임시 디렉토리: $TEMP_DIR"
          
          echo "버킷에서 파일 다운로드 중..."
          gcloud storage cp -r "gs://$BUCKET_NAME/$FOLDER_NAME" "$TEMP_DIR/"
          
          if [ ! -d "$TEMP_DIR/$FOLDER_NAME" ]; then
            echo "오류: 버킷에서 폴더를 찾을 수 없습니다: $FOLDER_NAME"
            exit 1
          fi
          
          echo "다운로드 완료. Worker VM으로 전송 중..."
          
          # VM에 /root/$ZKUP_DIR 디렉토리 생성 (존재하지 않으면)
          echo "Worker VM에 $ZKUP_DIR 디렉토리 확인 및 생성 중..."
          gcloud compute ssh "root@$VM_INSTANCE" --zone="$VM_ZONE" --command="mkdir -p /root/$ZKUP_DIR" --quiet
          
          # 파일을 VM으로 전송
          echo "파일을 Worker VM으로 전송 중..."
          if ! gcloud compute scp --recurse --zone="$VM_ZONE" "$TEMP_DIR/$FOLDER_NAME" "root@$VM_INSTANCE:/root/$ZKUP_DIR/"; then
            echo "오류: Worker VM으로 파일 전송 실패"
            exit 1
          fi
          
          echo "Worker VM 배포 완료"
          # 전송 확인
          echo "전송된 파일 확인 중..."
          if ! gcloud compute ssh "root@$VM_INSTANCE" --zone="$VM_ZONE" --command="ls -la /root/$ZKUP_DIR/$FOLDER_NAME" --quiet; then
            echo "경고: 전송 확인 실패 (파일은 전송되었을 수 있음)"
          fi
          
          # 임시 디렉토리 정리
          rm -rf "$TEMP_DIR"
          echo "임시 디렉토리 정리 완료"

      - name: Deploy from bucket to API Server VM
        run: |
          BUCKET_NAME=$(echo "${{ vars.GCP_BUCKET_NAME }}" | tr -d ' \n\r')
          VM_INSTANCE=$(echo "${{ vars.GCP_VM_ZKAP_API_SERVER_INSTANCE_NAME }}" | tr -d ' \n\r')
          VM_ZONE=$(echo "${{ vars.GCP_VM_ZONE }}" | tr -d ' \n\r')
          ZKUP_DIR=$(echo "${{ vars.GCP_VM_ZKUP_DIR_NAME }}" | tr -d ' \n\r')
          VERSION="${{ inputs.version }}"
          FOLDER_NAME="zkup_${VERSION}"
          
          echo "버킷에서 파일을 API Server VM으로 배포합니다..."
          echo "버킷: $BUCKET_NAME"
          echo "VM 인스턴스: $VM_INSTANCE"
          echo "VM Zone: $VM_ZONE"
          echo "버전: $VERSION"
          echo "폴더명: $FOLDER_NAME"
          
          # 임시 디렉토리에 버킷에서 파일 다운로드
          TEMP_DIR=$(mktemp -d)
          echo "임시 디렉토리: $TEMP_DIR"
          
          echo "버킷에서 파일 다운로드 중..."
          gcloud storage cp -r "gs://$BUCKET_NAME/$FOLDER_NAME" "$TEMP_DIR/"
          
          if [ ! -d "$TEMP_DIR/$FOLDER_NAME" ]; then
            echo "오류: 버킷에서 폴더를 찾을 수 없습니다: $FOLDER_NAME"
            exit 1
          fi
          
          echo "다운로드 완료. API Server VM으로 전송 중..."
          
          # VM에 /root/$ZKUP_DIR 디렉토리 생성 (존재하지 않으면)
          echo "API Server VM에 $ZKUP_DIR 디렉토리 확인 및 생성 중..."
          gcloud compute ssh "root@$VM_INSTANCE" --zone="$VM_ZONE" --command="mkdir -p /root/$ZKUP_DIR" --quiet
          
          # 파일을 VM으로 전송
          echo "파일을 API Server VM으로 전송 중..."
          if ! gcloud compute scp --recurse --zone="$VM_ZONE" "$TEMP_DIR/$FOLDER_NAME" "root@$VM_INSTANCE:/root/$ZKUP_DIR/"; then
            echo "오류: API Server VM으로 파일 전송 실패"
            exit 1
          fi
          
          echo "API Server VM 배포 완료"
          # 전송 확인
          echo "전송된 파일 확인 중..."
          if ! gcloud compute ssh "root@$VM_INSTANCE" --zone="$VM_ZONE" --command="ls -la /root/$ZKUP_DIR/$FOLDER_NAME" --quiet; then
            echo "경고: 전송 확인 실패 (파일은 전송되었을 수 있음)"
          fi
          
          # 임시 디렉토리 정리
          rm -rf "$TEMP_DIR"
          echo "임시 디렉토리 정리 완료"

  slack-notify-success:
    runs-on: self-hosted
    environment: development
    needs: [deploy-to-vm]
    if: success()
    steps:
      - name: Prepare Slack payload
        id: slack-payload
        env:
          VERSION: ${{ inputs.version }}
          WORKER_VM_INSTANCE: ${{ vars.GCP_VM_WORKER_INSTANCE_NAME }}
          API_SERVER_VM_INSTANCE: ${{ vars.GCP_VM_ZKAP_API_SERVER_INSTANCE_NAME }}
          BUCKET_NAME: ${{ vars.GCP_BUCKET_NAME }}
          ZKUP_DIR: ${{ vars.GCP_VM_ZKUP_DIR_NAME }}
          RUN_ID: ${{ github.run_id }}
        run: |
          # JSON payload 생성
          PAYLOAD=$(node -e "
            const version = process.env.VERSION || '';
            const workerVmInstance = process.env.WORKER_VM_INSTANCE || '';
            const apiServerVmInstance = process.env.API_SERVER_VM_INSTANCE || '';
            const bucketName = process.env.BUCKET_NAME || '';
            const zkupDir = process.env.ZKUP_DIR || '';
            const runId = process.env.RUN_ID || '';
            const text = \`✅ [개발] zkup VM 배포 완료\\n- 버전: \${version}\\n- Worker VM 인스턴스: \${workerVmInstance}\\n- API Server VM 인스턴스: \${apiServerVmInstance}\\n- 버킷: \${bucketName}\\n- 배포 위치: ~/\${zkupDir}/zkup_\${version}\\n- 배포가 성공적으로 완료되었습니다.\`;
            const payload = {
              text: '[개발] zkup VM 배포 알림',
              blocks: [
                {
                  type: 'section',
                  text: {
                    type: 'mrkdwn',
                    text: text
                  },
                  accessory: {
                    type: 'button',
                    text: {
                      type: 'plain_text',
                      text: '워크플로우 확인하기',
                      emoji: true
                    },
                    value: 'actions',
                    url: \`https://github.com/baerae-zkap/zkup/actions/runs/\${runId}\`,
                    action_id: 'workflow_check'
                  }
                }
              ]
            };
            console.log(JSON.stringify(payload));
          ")

          echo "payload<<EOF" >> $GITHUB_OUTPUT
          echo "$PAYLOAD" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: Greet the reader
        uses: slackapi/slack-github-action@v2.1.0  # TODO: pin to SHA — check https://github.com/slackapi/slack-github-action/releases/tag/v2.1.0
        with:
          webhook: ${{ secrets.SLACK_WEBHOOK_URL }}
          webhook-type: incoming-webhook
          payload: ${{ steps.slack-payload.outputs.payload }}

  slack-notify-failure:
    runs-on: self-hosted
    environment: development
    needs: [deploy-to-vm]
    if: failure()
    steps:
      - name: Prepare Slack failure payload
        id: slack-failure-payload
        env:
          VERSION: ${{ inputs.version }}
          WORKER_VM_INSTANCE: ${{ vars.GCP_VM_WORKER_INSTANCE_NAME }}
          API_SERVER_VM_INSTANCE: ${{ vars.GCP_VM_ZKAP_API_SERVER_INSTANCE_NAME }}
          BUCKET_NAME: ${{ vars.GCP_BUCKET_NAME }}
          RUN_ID: ${{ github.run_id }}
        run: |
          # JSON payload 생성
          PAYLOAD=$(node -e "
            const version = process.env.VERSION || '';
            const workerVmInstance = process.env.WORKER_VM_INSTANCE || '';
            const apiServerVmInstance = process.env.API_SERVER_VM_INSTANCE || '';
            const bucketName = process.env.BUCKET_NAME || '';
            const runId = process.env.RUN_ID || '';
            const text = \`❌ [개발] zkup VM 배포 실패\\n- 버전: \${version}\\n- Worker VM 인스턴스: \${workerVmInstance}\\n- API Server VM 인스턴스: \${apiServerVmInstance}\\n- 버킷: \${bucketName}\\n- 배포 작업이 실패했습니다.\\n- 상세 내용은 GitHub Actions에서 확인해주세요.\`;
            const payload = {
              text: '[개발] zkup VM 배포 실패 알림',
              blocks: [
                {
                  type: 'section',
                  text: {
                    type: 'mrkdwn',
                    text: text
                  },
                  accessory: {
                    type: 'button',
                    text: {
                      type: 'plain_text',
                      text: '워크플로우 확인하기',
                      emoji: true
                    },
                    value: 'actions',
                    url: \`https://github.com/baerae-zkap/zkup/actions/runs/\${runId}\`,
                    action_id: 'workflow_check'
                  }
                }
              ]
            };
            console.log(JSON.stringify(payload));
          ")

          echo "payload<<EOF" >> $GITHUB_OUTPUT
          echo "$PAYLOAD" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: Notify failure
        uses: slackapi/slack-github-action@v2.1.0  # TODO: pin to SHA — check https://github.com/slackapi/slack-github-action/releases/tag/v2.1.0
        with:
          webhook: ${{ secrets.SLACK_WEBHOOK_URL }}
          webhook-type: incoming-webhook
          payload: ${{ steps.slack-failure-payload.outputs.payload }}

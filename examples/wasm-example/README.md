# zkap-circuit WASM Example

Demonstrates using zkap-circuit in the browser via WebAssembly.

## Prerequisites
- Built WASM binding (run `wasm-pack build` in bindings/wasm/)

## Run
Open index.html in a browser (serve with a local HTTP server).
